# Python / tooling
__pycache__/
*.py[cod]
.venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Secrets: only .env.example is committed
.env
.env.*
!.env.example

# Personal Claude Code settings (permissions, paths)
.claude/settings.local.json

# Runtime data
/data/

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/

# Third-party design reference images (local use only, not for publication)
/docs/ui/references/
